use super::query::Query;

/// Compile a `.scm` query string into a `Query`.
pub fn compile_query(_source: &str) -> Query {
    // TODO: Phase 3.1 — parse query S-expression and build state machine
    unimplemented!("query compiler — see Phase 3.1 of the roadmap")
}
