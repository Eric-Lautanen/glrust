use glr_core::Grammar;

/// Compile a tree-sitter `grammar.json` file into a `Grammar` struct.
pub fn compile_grammar(_json_path: &str) -> Grammar {
    // TODO: Phase 2.2 — parse grammar.json, compute LR items, emit parse table
    unimplemented!("grammar.json compiler — see Phase 2.2 of the roadmap")
}
