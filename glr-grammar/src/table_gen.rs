use glr_core::ParseTable;

/// Generates LR(1) parse tables from grammar definitions.
pub struct ParseTableGenerator;

impl ParseTableGenerator {
    pub fn generate() -> ParseTable {
        // TODO: Phase 2.2 — compute LR(1) items, build ACTION/GOTO tables
        unimplemented!("LR table generation — see Phase 2.2 of the roadmap")
    }
}
