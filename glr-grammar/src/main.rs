fn main() {
    // TODO: Phase 2.2 — CLI for compiling grammar.json → grammar.bin
    // Usage: cargo run -- compile path/to/grammar.json -o grammar.bin
    eprintln!("glr-grammar compiler — see Phase 2.2 of the roadmap");
    std::process::exit(1);
}
